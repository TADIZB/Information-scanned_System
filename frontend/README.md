# Frontend (React + Vite)

## Setup
```powershell
npm install
```

## Run
```powershell
npm run dev
```

## Notes
- Expect backend at `http://localhost:8000`
- Camera requires HTTPS in production; localhost is allowed.
